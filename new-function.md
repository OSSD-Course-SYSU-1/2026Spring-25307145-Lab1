```python 
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""

"""

md_content = """# 📱 ToDo List 应用功能文档

## 一、任务管理

### 1.1 添加任务
- 点击底部 **"+"** 按钮打开添加对话框
- 填写以下信息：
  - **任务名称**（必填）
  - **备注**（选填）
  - **优先级**：Low / Medium / High
  - **截止日期**：通过开关启用，选择年月日和时间
  - **类别**：选择已有类别或留空
- 点击 **Confirm** 确认添加

### 1.2 编辑任务
- 点击任务旁的 **Edit** 按钮进入内联编辑模式
- 直接修改任务名称
- 点击确认图标保存修改

### 1.3 完成任务
- 点击任务左侧的**复选框**
- 任务自动标记为已完成（名称显示删除线）
- 从未完成列表移到已完成列表

### 1.4 删除任务
- 滑动任务项露出**删除按钮**
- 或点击任务右侧的**删除图标**
- 删除后提示 "Deleted"

### 1.5 优先级切换
- 任务左侧有**颜色圆点**标识优先级：
  - 🟢 **Low** — 绿色 `#2ECC71`
  - 🟠 **Medium** — 橙色 `#F39C12`
  - 🔴 **High** — 红色 `#E74C3C`
- 点击圆点可循环切换优先级

### 1.6 搜索任务
- 顶部**搜索栏**按名称过滤任务
- 支持大小写不敏感的模糊匹配

---

## 二、截止日期

- 通过 **Due Date 开关**启用/禁用
- 启用后显示三个独立的**滚轮选择器**：
  - **年**：2020–2040 年
  - **月**：1–12 月
  - **日**：1–31 日
- 年月日可**独立滑动选择**，互不干扰
- 同时可选择**时:分**（TimePicker）

---

## 三、类别管理

### 3.1 查看类别
- 类别以**彩色标签**形式展示在搜索栏下方
- 每个类别有独立的名称和颜色

### 3.2 添加类别
- 点击标签栏末尾的 **"+"** 按钮
- 输入类别名称
- 从预设颜色列表中选一个颜色
- 点击 **Add** 确认

### 3.3 编辑类别
- **长按**类别标签（约 500ms）
- 弹出选项框，点击 **Edit**
- 修改类别名称或颜色
- 点击 **Save** 保存

### 3.4 删除类别
- **长按**类别标签
- 弹出选项框，点击 **Delete**
- 引用该类别的任务将自动清空类别

### 3.5 按类别筛选
- **点击**类别标签切换筛选
- 选中后只显示该类别的任务
- 点击 **All** 取消筛选

---

## 四、一键清除

| 区域 | 功能 | 说明 |
|------|------|------|
| **未完成** | `Clear` 按钮 | 一键清除所有未完成任务 |
| **已完成** | `Clear` 按钮 | 一键清除所有已完成任务 |

- 点击 Clear 后会弹出**确认对话框**，防止误操作

---

## 五、响应式布局

根据屏幕宽度自动切换布局：

### 📱 手机模式（宽度 < 600px）
┌──────────────────┐
│ 未完成列表 │
│ ┌──────────────┐ │
│ │ 任务 1 │ │
│ │ 任务 2 │ │
│ └──────────────┘ │
│ 已完成列表 │
│ ┌──────────────┐ │
│ │ 任务 3 (完成) │ │
│ └──────────────┘ │
└──────────────────┘

### 📟 平板模式（宽度 ≥ 600px）
┌──────────────────┬──────────────────┐
│ 未完成 │ 已完成 │
│ ┌────────────┐ │ ┌────────────┐ │
│ │ 任务 1 │ │ │ 任务 3 (完成) │ │
│ │ 任务 2 │ │ │ │ │
│ └────────────┘ │ └────────────┘ │
└──────────────────┴──────────────────┘

---

## 六、数据持久化

- 使用 HarmonyOS **RdbStore**（关系型数据库）存储数据
- 数据表：
  - `todo` — 存储任务（ID、名称、完成状态、优先级、截止日期、类别、备注、创建/更新时间）
  - `category` — 存储类别（ID、名称、颜色）
- 应用重启后数据保留
- 首次运行自动创建**默认类别**：Work、Personal、Study、Shopping、Other

---

## 七、跨设备迁移

- 支持 HarmonyOS **自由流转 / ContinuationState**
- 搜索文本和类别筛选状态可在设备间同步

---

## 八、技术栈

| 项目 | 内容 |
|------|------|
| **框架** | HarmonyOS ArkUI（Stage 模型） |
| **语言** | ArkTS（TypeScript 子集） |
| **数据存储** | `@kit.ArkData` — RdbStore |
| **目标 SDK** | HarmonyOS 5.0.5 (17) |
| **设备类型** | Phone |
"""``` 
