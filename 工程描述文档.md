# 工程描述文档

## 一、工程概览

| 项目 | 内容 |
|------|------|
| **工程名称** | ListItemEdit（待办事项列表编辑） |
| **包名** | `com.example.listitemedit` |
| **版本** | 1.0.0 (versionCode: 1000000) |
| **SDK版本** | HarmonyOS 5.0.5 Release (API 17) |
| **运行OS** | HarmonyOS |
| **设备类型** | phone（手机） |
| **开发框架** | ArkTS（Stage模型） |
| **工程类型** | 元服务/应用（entry类型模块） |

---

## 二、目录结构总览

```
list-item-edit-master/
├── AppScope/                                    # 应用全局配置
│   ├── app.json5                                # 应用级配置（包名、图标、标签等）
│   └── resources/base/
│       ├── element/string.json                  # 应用名称字符串资源
│       └── media/                               # 应用图标资源
├── entry/                                       # 主模块（entry类型）
│   ├── build-profile.json5                      # 模块构建配置
│   ├── hvigorfile.ts                            # 模块级Hvigor构建脚本
│   ├── obfuscation-rules.txt                    # 代码混淆规则
│   ├── oh-package.json5                         # 模块包依赖配置
│   └── src/main/
│       ├── module.json5                         # 模块配置文件
│       ├── ets/                                 # ArkTS源代码目录
│       │   ├── common/Constants.ets             # 公共常量和样式配置
│       │   ├── entryability/EntryAbility.ets    # Ability入口
│       │   ├── model/ToDo.ets                   # 待办事项数据模型
│       │   ├── pages/Index.ets                  # 主页面
│       │   └── view/TodoListItem.ets            # 待办事项列表项组件
│       └── resources/                           # 多语言资源目录
│           ├── base/                            # 默认（英文）资源
│           │   ├── element/color.json           # 颜色资源
│           │   ├── element/string.json          # 字符串资源
│           │   ├── element/stringarray.json     # 字符串数组资源
│           │   ├── media/                       # 图片资源
│           │   └── profile/main_pages.json      # 页面路由配置
│           ├── en_US/                           # 英文（美国）资源
│           │   └── element/string.json          # 英文字符串
│           │   └── element/stringarray.json     # 英文字符串数组
│           └── zh_CN/                           # 中文（简体）资源
│               └── element/string.json          # 中文字符串
│               └── element/stringarray.json     # 中文字符串数组
├── hvigor/                                      # Hvigor构建系统配置
│   └── hvigor-config.json5                      # 全局构建配置
├── build-profile.json5                          # 应用级构建配置
├── hvigorfile.ts                                # 应用级Hvigor构建脚本
├── oh-package.json5                             # 工程级包依赖配置
├── LICENSE                                      # 开源许可
├── README.md                                    # 中文README
└── README.en.md                                 # 英文README
```

---

## 三、配置文件详解

### 3.1 工程级配置文件

#### `oh-package.json5`（工程包配置）

```json5
{
  "modelVersion": "5.0.0",
  "name": "listitemedit",
  "version": "1.0.0",
  "description": "Please describe the basic information."
}
```

- **modelVersion**: OHPM包模型版本 5.0.0
- **name**: 工程名称 `listitemedit`
- **version**: 版本号 `1.0.0`

#### `build-profile.json5`（应用构建配置）

- **兼容SDK版本**: `5.0.5(17)` — HarmonyOS 5.0.5 Release
- **目标SDK版本**: `5.0.5(17)`
- **运行OS**: HarmonyOS
- **构建模式**: debug 和 release
- **模块**: 仅包含 `entry` 模块

#### `hvigor/hvigor-config.json5`（Hvigor构建引擎配置）

- **modelVersion**: 5.0.0
- 包含执行配置（分析模式、守护进程、增量编译、并行编译、类型检查等）—— 均为注释状态，使用默认值
- 包含日志和调试配置

#### `hvigorfile.ts`（构建脚本入口）

- 使用 `@ohos/hvigor-ohos-plugin` 内置插件 `appTasks`

---

### 3.2 应用级配置（AppScope）

#### `AppScope/app.json5`

```json5
{
  "app": {
    "bundleName": "com.example.listitemedit",
    "vendor": "example",
    "versionCode": 1000000,
    "versionName": "1.0.0",
    "icon": "$media:app_icon",
    "label": "$string:app_name"
  }
}
```

| 属性 | 值 | 说明 |
|------|-----|------|
| bundleName | `com.example.listitemedit` | 应用包名，唯一标识 |
| vendor | `example` | 供应商 |
| versionCode | 1000000 | 版本号（数字） |
| versionName | 1.0.0 | 版本名 |
| icon | `$media:app_icon` | 应用图标 |
| label | `$string:app_name` | 应用名称（"ListItemEdit"） |

---

### 3.3 模块级配置（entry）

#### `entry/src/main/module.json5`

```json5
{
  "module": {
    "name": "entry",
    "type": "entry",
    "mainElement": "EntryAbility",
    "deviceTypes": ["phone"],
    "pages": "$profile:main_pages",
    "abilities": [
      {
        "name": "EntryAbility",
        "srcEntry": "./ets/entryability/EntryAbility.ets",
        "exported": true,
        "skills": [
          {
            "entities": ["entity.system.home"],
            "actions": ["action.system.home"]
          }
        ]
      }
    ]
  }
}
```

- **模块类型**: entry（应用入口模块）
- **支持设备**: phone（手机）
- **Ability**: 仅有一个 `EntryAbility`，作为应用主入口
- **Skills**: 配置了桌面启动（Home Screen）意图

#### `entry/build-profile.json5`

- **apiType**: `stageMode`（Stage模型）
- **targets**: default

#### `entry/oh-package.json5`

- 模块名: `entry`，版本 `1.0.0`，无第三方依赖

---

## 四、源代码文件详解

### 4.1 `common/Constants.ets` — 公共常量与样式配置

**文件路径**: `entry/src/main/ets/common/Constants.ets`

**功能说明**: 定义项目中使用的所有样式常量值和全局常量。

**导出内容**:

| 导出名 | 类型 | 说明 |
|--------|------|------|
| `STYLE_CONFIG` | `Record<string, number>` | 样式配置对象，包含所有UI尺寸常量 |
| `Constant` | `class` | 全局常量类，包含百分比值 |

**STYLE_CONFIG 常量表**:

| 常量名 | 值 | 用途 |
|--------|-----|------|
| IMAGE_SIZE | 32 | 操作栏图标尺寸 |
| IMAGE_MARGIN | 4 | 图标外边距 |
| ICON_GUTTER | 4 | 图标间距 |
| OPERATION_BUTTON_PADDING | 4 | 操作按钮内边距 |
| MENU_IMAGE_SIZE | 24 | 菜单图标尺寸 |
| MENU_HEIGHT | 56 | 菜单高度 |
| TEXT_MARGIN_LEFT | 40 | 文本左外边距 |
| IMAGE_MARGIN_RIGHT | 30 | 图片右外边距 |
| LIST_ITEM_GUTTER | 12 | 列表项间距 |
| BORDER_WIDTH | 3 | 边框宽度 |
| TODO_ITEM_HEIGHT | 80 | 待办项高度 |
| TODO_ITEM_PADDING_VERTICAL | 4 | 待办项垂直内边距 |
| IMAGE_ICON_OK_SIZE | 15 | 完成图标尺寸 |
| ANIMATION_DURATION | 600 | 动画时长(ms) |
| CUSTOM_CHECKBOX_SIZE | 20 | 自定义复选框尺寸 |

**Constant 类常量**:

| 常量名 | 值 | 说明 |
|--------|-----|------|
| PERCENT_12 | '12%' | 12% |
| PERCENT_FULL | '100%' | 100% |

---

### 4.2 `model/ToDo.ets` — 待办事项数据模型

**文件路径**: `entry/src/main/ets/model/ToDo.ets`

**功能说明**: 定义待办事项的数据结构，使用 `@Observed` 装饰器支持深度响应式绑定。

**类定义**:

```typescript
@Observed
export class ToDo {
  key: string = util.generateRandomUUID(true);  // 唯一标识（UUID）
  name: string;                                  // 待办事项名称
  isCompleted: boolean = false;                  // 是否已完成
}
```

**关键设计**:
- `@Observed`: 使类的属性变化能被UI观察并自动刷新
- `key`: 使用 `util.generateRandomUUID(true)` 生成随机UUID，作为列表渲染的 `key` 值
- `name`: 待办事项的文本内容
- `isCompleted`: 标记是否已完成，默认 `false`

---

### 4.3 `entryability/EntryAbility.ets` — Ability 入口

**文件路径**: `entry/src/main/ets/entryability/EntryAbility.ets`

**功能说明**: 应用的主Ability（UIAbility），负责应用生命周期管理和页面加载。

**生命周期回调**:
| 方法 | 说明 |
|------|------|
| `onCreate` | Ability创建时调用，打印日志 |
| `onDestroy` | Ability销毁时调用 |
| `onWindowStageCreate` | 窗口阶段创建时，加载首页 `pages/Index` |
| `onWindowStageDestroy` | 窗口阶段销毁时 |
| `onForeground` | 切换到前台时 |
| `onBackground` | 切换到后台时 |

**依赖库**:
- `@kit.AbilityKit` — Ability框架
- `@kit.PerformanceAnalysisKit` — 日志（hilog）
- `@kit.ArkUI` — 窗口管理

---

### 4.4 `pages/Index.ets` — 主页面

**文件路径**: `entry/src/main/ets/pages/Index.ets`

**功能说明**: 应用的主界面，使用 `List` 组件实现待办事项列表，支持新增、完成标记、左滑删除等交互。

**组件结构**: `ToDoList`（`@Entry @Component`）

**状态管理**:
| 状态变量 | 类型 | 说明 |
|----------|------|------|
| `toDoData` | `ToDo[]` | 未完成的待办事项列表 |
| `achieveData` | `ToDo[]` | 已完成的待办事项列表 |

**核心功能**:

1. **新增待办事项**
   - 点击顶部的 **"+" 添加按钮**
   - 弹出 `showTextPickerDialog` 文本选择器
   - 从 `stringarray.json` 中的 `available_things` 数组选择
   - 选中后通过 `unshift` 插入到 `toDoData` 列表头部

2. **左滑操作栏**（`itemEnd` @Builder）
   - 使用 `swipeAction` 属性实现左滑
   - 包含三个操作按钮：
     - ⚙️ **设置图标**: 提示"功能待完善"
     - 📋 **详情图标**: 提示"功能待完善"
     - 🗑️ **删除图标**: 调用 `deleteTodoItem` 删除

3. **删除待办事项**（`deleteTodoItem`）
   - 根据 `isCompleted` 判断从 `toDoData` 或 `achieveData` 中移除
   - 通过 `filter` 方法过滤
   - 显示 `showToast` 提示"删除成功"

4. **列表分组展示**
   - 顶部标题栏显示"To-Do List"
   - "Ongoing（未完成）" 分区标题
   - 已完成项目显示在 "Completed（已完成）" 分区
   - 使用 `ForEach` 循环渲染列表项

**UI 布局**:
- 整体背景色: `#F1F3F5`（浅灰色）
- 列表项组件: `TodoListItem`（自定义组件）
- 每个列表项均有左滑操作手势

---

### 4.5 `view/TodoListItem.ets` — 待办事项列表项组件

**文件路径**: `entry/src/main/ets/view/TodoListItem.ets`

**功能说明**: 单个待办事项的UI组件，支持完成状态切换和原地编辑。

**组件装饰器**: `@Component`

**输入/状态**:
| 变量 | 装饰器 | 说明 |
|------|--------|------|
| `toDoItem` | `@ObjectLink` | 待办事项数据（深层响应式） |
| `toDoData` | `@Link` | 父组件传来的未完成列表引用 |
| `achieveData` | `@Link` | 父组件传来的已完成列表引用 |
| `isEdited` | `@State` | 是否处于编辑模式 |

**核心功能**:

1. **完成状态切换**（`addAchieveData`）
   - 点击自定义复选框切换 `isCompleted`
   - 使用 `animateTo` 实现平滑动画（600ms）
   - 已完成 → 从未完成列表移入已完成列表
   - 未完成 → 从已完成列表移回未完成列表

2. **编辑模式**
   - 点击"Edit"文字进入编辑模式
   - 编辑模式：显示 `TextInput` 输入框替代普通文本
   - 输入框自动获取焦点（`focusControl.requestFocus`）
   - 编辑完成后点击确认图标（✓）退出编辑模式

3. **UI 展示**
   - 自定义复选框：圆形样式，完成时填充主题色并显示 ✓ 图标
   - 已完成事项：文字显示删除线（`TextDecorationType.LineThrough`）
   - 列表项圆角背景（白色卡片样式）

---

## 五、资源文件详解

### 5.1 颜色资源（`color.json`）

| 名称 | 值 | 用途 |
|------|-----|------|
| `start_window_background` | `#FFFFFF` | 启动窗口背景色（白色） |
| `background_color` | `#F1F3F5` | 页面背景色（浅灰） |

### 5.2 字符串资源（`string.json`）

**base/en_US（英文）**:

| 名称 | 值 |
|------|-----|
| app_name | ListItemEdit |
| module_desc | module description |
| EntryAbility_desc | description |
| EntryAbility_label | ListItemEdit |
| edit | Edit |
| deleted | Successfully deleted |
| incomplete | Incomplete |
| todo | To-Do List |
| undo | Ongoing |
| done | Completed |

**zh_CN（中文）**:

| 名称 | 值 |
|------|-----|
| app_name | ListItemEdit |
| module_desc | 模块描述 |
| EntryAbility_label | 列表项编辑 |
| edit | 编辑 |
| deleted | 删除成功 |
| incomplete | 功能待完善 |
| todo | 待办 |
| undo | 未完成 |
| done | 已完成 |

### 5.3 字符串数组资源（`stringarray.json`）

预定义的待办事项选项（用于添加时的选择器）：

| 语言 | 选项 |
|------|------|
| en_US | Read books, Sports, Travel, Listening to music, Watch movies, Singing |
| zh_CN | 读书, 运动, 旅游, 听音乐, 看电影, 唱歌 |

### 5.4 页面路由配置（`main_pages.json`）

```json
{
  "src": ["pages/Index"]
}
```

仅包含一个页面路由：`pages/Index`。

### 5.5 分层图标配置（`layered_image.json`）

```json
{
  "layered-image": {
    "background": "$media:background",
    "foreground": "$media:foreground"
  }
}
```

使用分层图标技术，背景层和前景层分开管理。

---

## 六、混淆配置（`obfuscation-rules.txt`）

启用的混淆选项：

| 选项 | 说明 |
|------|------|
| `-enable-property-obfuscation` | 混淆属性名称 |
| `-enable-toplevel-obfuscation` | 混淆全局作用域名称 |
| `-enable-filename-obfuscation` | 混淆文件名 |
| `-enable-export-obfuscation` | 混淆导出名称 |

---

## 七、依赖关系分析

### 7.1 模块依赖图

```
entry 模块
  ├── @kit.AbilityKit          (EntryAbility)
  ├── @kit.PerformanceAnalysisKit  (EntryAbility, Index)
  ├── @kit.ArkUI               (EntryAbility)
  ├── @kit.BasicServicesKit    (Index - BusinessError)
  └── @kit.ArkTS               (ToDo - util)
```

### 7.2 组件依赖关系

```
Index (ToDoList)
  ├── ToDo (数据模型)
  ├── TodoListItem (列表项组件)
  │     ├── ToDo (数据模型)
  │     └── Constants (常量配置)
  └── Constants (常量配置)
```

---

## 八、功能流程总结

### 功能列表

| 功能 | 实现方式 | 状态 |
|------|----------|------|
| 展示待办事项列表 | `List` + `ForEach` | ✅ 已完成 |
| 新增待办事项 | 文本选择器对话框 + `unshift`插入 | ✅ 已完成 |
| 标记完成 | 自定义复选框 + `animateTo`动画 | ✅ 已完成 |
| 原地编辑待办事项名称 | `TextInput` + `focusControl` | ✅ 已完成 |
| 左滑删除 | `swipeAction` + `filter`移除 | ✅ 已完成 |
| 左滑操作栏 | 设置/详情/删除三个图标按钮 | ✅ 已完成 |
| 中英文国际化 | base/en_US/zh_CN 三套资源 | ✅ 已完成 |

### 用户交互流程

```
启动应用 → 展示待办列表（空）
    ↓
点击"+"按钮 → 弹出选择器 → 选择事项 → 添加到列表顶部
    ↓
点击复选框 → 动画过渡 → 从未完成移至已完成
    ↓
左滑项目 → 显示操作栏 → 点击删除 → 移除该项目
    ↓
点击"Edit" → 进入编辑模式 → 修改名称 → 点击✓确认
```

---

## 九、技术要点总结

1. **Stage模型**: 采用 HarmonyOS Stage 模型开发，使用 `UIAbility` 管理应用生命周期。
2. **响应式编程**: 大量使用 `@State`、`@Link`、`@ObjectLink`、`@Observed` 装饰器实现数据驱动的UI刷新。
3. **List组件高级用法**: 使用 `swipeAction` 实现左滑操作栏、`EdgeEffect.Spring` 弹性滚动效果。
4. **动画处理**: 使用 `animateTo` 实现列表项状态切换的平滑过渡动画。
5. **系统对话框**: 使用 `showTextPickerDialog` 实现待办事项选择，`showToast` 实现操作反馈。
6. **国际化**: 支持 base（默认）、en_US、zh_CN 三套资源，根据系统语言自动适配。
7. **代码混淆**: 已配置属性、顶级作用域、文件名和导出的全面混淆保护。
